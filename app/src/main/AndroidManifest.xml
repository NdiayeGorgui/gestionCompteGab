<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetatm_gorgui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetATM_Gorgui">
        <activity android:name=".Payer_InteretActivity"></activity>
        <activity android:name=".Compte_EpargneActivity" />
        <activity android:name=".ClientsActivity" />
        <activity android:name=".Comptes_ChequeActivity" />
        <activity android:name=".Ecran_AdminActivity" />
        <activity android:name=".view.Guichet_PortraitActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>